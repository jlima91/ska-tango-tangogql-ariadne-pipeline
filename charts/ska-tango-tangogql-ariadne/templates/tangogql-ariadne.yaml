---
apiVersion: v1
kind: Service
metadata:
  name: {{ template "tangogql-ariadne.name" . }}-{{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ template "tangogql-ariadne.name" . }}-{{ .Release.Name }}
    chart: {{ template "tangogql-ariadne.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: ClusterIP
  ports:
  - name: tangogql-ariadne-tcp
    port: 5004
    targetPort: tangogql-ariadne-tcp
    protocol: TCP
  selector:
    app: {{ template "tangogql-ariadne.name" . }}-{{ .Release.Name }}

---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ template "tangogql-ariadne.name" . }}-{{ .Release.Name }}
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ template "tangogql-ariadne.name" . }}-{{ .Release.Name }}
    chart: {{ template "tangogql-ariadne.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  selector:
    matchLabels:
      app: {{ template "tangogql-ariadne.name" . }}-{{ .Release.Name }}
  serviceName: {{ template "tangogql-ariadne.name" . }}-{{ .Release.Name }}
  replicas: {{ .Values.tangogql-ariadne.replicas }}
  template:
    metadata:
      labels:
        app: {{ template "tangogql-ariadne.name" . }}-{{ .Release.Name }}
        chart: {{ template "tangogql-ariadne.chart" . }}
        release: {{ .Release.Name }}
        heritage: {{ .Release.Service }}
    spec:
      initContainers:
      - name: waitdatabaseds
        image: "{{ .Values.itango.image.registry }}/{{ .Values.itango.image.image }}:{{ .Values.itango.image.tag }}"
        imagePullPolicy: IfNotPresent
        command:
          - /usr/local/bin/wait-for-it.sh
          - {{ .Values.global.tango_host }}
          - --timeout=30
          - --strict
          - --
          - echo "Databaseds ready for use."
      containers:
      - name: tangogql-ariadne
        image: "{{ .Values.tangogql-ariadne.image.registry }}/{{ .Values.tangogql-ariadne.image.image }}:{{ .Values.tangogql-ariadne.image.tag }}"
        imagePullPolicy: {{ .Values.tangogql-ariadne.image.pullPolicy }}
        volumeMounts:
        - mountPath: /var/log/tangogql-ariadne
          name: tangogql-ariadne-logs
        - name: logging-config
          mountPath: /app/logging.yaml
          subPath: logging.yaml
        env:
          - name: LOG_PATH
            value: "/var/log/tangogql-ariadne"
          - name: PYTHONUNBUFFERED
            value: "1"
          - name: TANGO_HOST
            value: {{ .Values.global.tango_host }}
        ports:
        - name: tangogql-ariadne-tcp
          containerPort: 5004
          protocol: TCP
        resources:
{{ toYaml .Values.tangogql-ariadne.resources | indent 10 }}

      volumes:
      - name: logging-config
        configMap:
          name: config-{{ template "tangogql-ariadne.name" . }}-{{ .Release.Name }}
      - name: tangogql-ariadne-logs
        persistentVolumeClaim:
          claimName: {{ template "tangogql-ariadne.name" . }}-{{ .Release.Name }}
{{- with .Values.nodeSelector }}
      nodeSelector:
{{ toYaml . | indent 8 }}
{{- end }}
{{- with .Values.affinity }}
      affinity:
{{ toYaml . | indent 8 }}
{{- end }}
{{- with .Values.tolerations }}
      tolerations:
{{ toYaml . | indent 8 }}
{{- end }}
